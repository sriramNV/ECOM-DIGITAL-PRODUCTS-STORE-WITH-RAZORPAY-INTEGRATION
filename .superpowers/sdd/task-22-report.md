# Task 22 Report — Printify order submission module

**Status:** ✅ Complete

**Created:** `apps/web/lib/printify/orders.ts`

**Module exports:**
- `printifyOrders.submit(data)` — POST an order to Printify
- `printifyOrders.getStatus(orderId)` — GET order status
- `printifyOrders.calculateShipping(lineItems, address)` — POST shipping calculation

**Commit:** `1abca81` — "feat: add Printify order submission module"
