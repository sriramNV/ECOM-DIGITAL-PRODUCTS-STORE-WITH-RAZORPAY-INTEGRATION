# Required — generate with: openssl rand -hex 32
POSTGRES_PASSWORD=
REDIS_PASSWORD=
MINIO_ROOT_PASSWORD=
AUTH_SECRET=

# Optional — change for your domain
AUTH_URL=http://localhost:3000
AUTH_TRUST_HOST=false
